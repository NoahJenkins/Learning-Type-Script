let myName = "Noah"
// Note that Typescript automatically infers that my datatype is a string

let myName2: string = "Also Noah"
// we can also set a var type manually using a colon after the var name and the putting the var type. 

let numberOfWheels: number = 4
let isStudent: boolean = false